# Runtime dependencies are declared once, in pyproject.toml, so the two cannot
# drift apart. This file stays so that `pip install -r requirements.txt` keeps
# working for anyone who reaches for it out of habit.
#
#   pip install -e .            # runtime
#   pip install -e ".[live]"    # runtime + the webcam dashboard
-e .
