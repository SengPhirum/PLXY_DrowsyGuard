# 16 MB layout for the N16R8 board.
#
# The app partition is oversized on purpose: ESP-DL's default model location is
# CONFIG_*_MODEL_IN_FLASH_RODATA, which links msr_s8_v1 + mnp_s8_v1 + the eye model
# into the binary. `assets` holds the recorded voice clips (assets/audio/) once the
# I2S path is wired.
#
# Name,     Type, SubType, Offset,   Size
nvs,        data, nvs,     0x9000,   0x6000
phy_init,   data, phy,     0xf000,   0x1000
factory,    app,  factory, 0x10000,  6M
assets,     data, spiffs,  ,         4M
